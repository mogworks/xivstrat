services:
  xivstrat:
    image: caddy:alpine
    container_name: xivstrat
    restart: unless-stopped
    network_mode: host
    volumes:
      - /root/xivstrat/conf:/etc/caddy
      - /root/xivstrat/dist:/srv
