services:
  xivstrat:
    image: caddy:alpine
    container_name: xivstrat
    restart: unless-stopped
    volumes:
      - /root/xivstrat/conf:/etc/caddy
      - /root/xivstrat/dist:/srv
    network_mode: host
