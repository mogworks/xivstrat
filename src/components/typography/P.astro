---
import { cn } from '@/lib/utils'

interface Props {
  class?: string
  [key: string]: any
}

const { class: className, ...rest } = Astro.props
---

<div class={cn('paragraph', className)} {...rest}>
  <slot />
</div>
