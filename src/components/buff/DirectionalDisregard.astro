---
import Buff from '@/components/Buff.astro'

interface Props {
  tag?: string
  hover?: boolean
  class?: string
}

const { tag = '', hover = false, class: className = '' }: Props = Astro.props
const icon = 'buff/214224.webp'
---

<Buff
  icon={icon}
  name="免除方向要求"
  nameVariant="yellow"
  description="具有方向要求的技能击中时默认方向正确"
  tag={tag}
  hover={hover}
  class={className}
/>
