---
import Buff from '@/components/Buff.astro'

import icon from '@/assets/buff/215519.png'

interface Props {
  tag?: string
  hover?: boolean
  class?: string
}

const { tag = '', hover = false, class: className = '' }: Props = Astro.props
---

<Buff
  icon={icon}
  name="伤害提高"
  nameVariant="yellow"
  description="攻击所造成的伤害提高"
  tag={tag}
  hover={hover}
  class={className}
/>
