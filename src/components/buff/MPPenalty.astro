---
import Buff from '@/components/Buff.astro'

interface Props {
  tag?: string
  hover?: boolean
  class?: string
}

const { tag = '', hover = false, class: className = '' }: Props = Astro.props
const icon = 'buff/215078.webp'
---

<Buff
  icon={icon}
  name="魔力衰减"
  nameVariant="yellow"
  description="魔力最大值降低"
  tag={tag}
  hover={hover}
  class={className}
/>
