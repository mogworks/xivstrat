---
import Action from './Action.astro'

import action_000861 from '../_assets/000861.png'

interface Props {
  duty: string
}

const { duty }: Props = Astro.props
---

<Action duty={duty} icon={action_000861} role="D4" recast={90} duration={15} physical={95} magical={90} special={100} />
