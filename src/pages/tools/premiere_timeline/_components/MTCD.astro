---
import Action from './Action.astro'

import action_000806 from '../_assets/000806.png'

interface Props {
  duty: string
}

const { duty }: Props = Astro.props
---

<Action duty={duty} icon={action_000806} role="MT" recast={60} duration={15} physical={90} magical={90} special={90} />
