---
import type { Time } from '@/lib/utils'

import { Image } from 'astro:assets'

import RoleIcon from '@/components/RoleIcon.astro'
import Span from '@/components/Span.vue'
import MechanicSection from '@/components/section/MechanicSection.astro'
import SolutionSection from '@/components/section/SolutionSection.astro'

import AlexandrianHolyCast from '../_components/AlexandrianHolyCast.astro'

import img from '@/assets/07/zelenia/Tmp.png'

interface Props {
  start: Time
}

const { start }: Props = Astro.props
---

<AlexandrianHolyCast start={start} />

<MechanicSection>
  <div class="flex gap-4">
    <div class="max-w-88 flex-1">
      <Image src={img} alt="" class="rounded border-2 border-fuchsia-400 dark:border-fuchsia-100" />
    </div>
    <div class="flex-2 space-y-4 rounded-md border border-dashed p-4">
      <div class="paragraph">
        在场地中 召唤
        <Span variant="blue">需要踩的塔</Span>x4
      </div>
    </div>
  </div>
</MechanicSection>
<SolutionSection>
  <div class="flex flex-col gap-4">
    <div class="paragraph">
      被点<Span variant="pink">钢铁</Span>
      的玩家需要站在空地放置
      <Span variant="pink">钢铁</Span>
      ，生效后迅速离开
      <Span variant="pink">钢铁</Span>
      的范围
    </div>
    <div class="paragraph">
      被点<Span variant="blue">月环</Span>
      的玩家需要踩
      <Span variant="blue">塔</Span>
      ，并保持不动，直到<Span variant="blue">月环</Span>消失
    </div>
    <div class="flex gap-4">
      <div class="flex max-w-88 shrink grow flex-col items-center gap-2">
        <Image src={img} alt="" class="rounded border-2 border-fuchsia-400 dark:border-fuchsia-100" />

        <div class="paragraph">
          <RoleIcon role="tank" />
          <RoleIcon role="healer" />
          被点
          <Span variant="blue">月环</Span>
        </div>
      </div>
      <div class="flex max-w-88 shrink grow flex-col items-center gap-2">
        <Image src={img} alt="" class="rounded border-2 border-fuchsia-400 dark:border-fuchsia-100" />

        <div class="paragraph">
          <RoleIcon role="dps" />
          被点
          <Span variant="blue">月环</Span>
        </div>
      </div>
    </div>
  </div>
</SolutionSection>
