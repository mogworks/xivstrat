---
import Buff from '@/components/Buff.astro'

import in_the_spotlight from '@/assets/buff/07/m5s/214284.png'
import { translations } from '../_translations'

interface Props {
  tag?: string
  hover?: boolean
  class?: string
}

const { tag = '', hover = false, class: className = '' }: Props = Astro.props
---

<Buff
  icon={in_the_spotlight}
  name={translations.inTheSpotLight}
  nameVariant="yellow"
  description={translations.inTheSpotLightDesc}
  tag={tag}
  hover={hover}
  class={className}
/>
