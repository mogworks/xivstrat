---
import Buff from '@/components/Buff.astro'

import burn_baby_burn from '@/assets/buff/07/m5s/214280.png'
import { translations } from '../_translations/index'

interface Props {
  tag?: string
  hover?: boolean
  class?: string
}

const { tag = '', hover = false, class: className = '' }: Props = Astro.props
---

<Buff
  icon={burn_baby_burn}
  name={translations.burnBabyBurn}
  nameVariant="yellow"
  description={translations.burnBabyBurnDesc}
  tag={tag}
  hover={hover}
  class={className}
/>
