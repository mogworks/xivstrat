---
import Buff from '@/components/Buff.astro'

import burning_up from '@/assets/buff/07/m6s/214288.png'

interface Props {
  tag?: string
  hover?: boolean
  class?: string
}

const { tag = '', hover = false, class: className = '' }: Props = Astro.props
---

<Buff
  icon={burning_up}
  name="Burning Up"
  nameVariant="yellow"
  description="Getting hotter by the moment, resulting in damage to those nearby when this effect expires."
  tag={tag}
  hover={hover}
  class={className}
/>
