---
import type { Time } from '@/lib/utils'

import CastSection from '@/components/section/CastSection.astro'

import HowlingBlade from '../_components/HowlingBlade.astro'

interface Props {
  start: Time
  base?: Time
  [key: string]: any
}

const { start, base = 0, ...rest }: Props = Astro.props
const ability = '大地の呼び声' //召唤柱子
---

<CastSection start={start} base={base} duration={3.7} ability={ability} {...rest}>
  <HowlingBlade slot="source" />
  柱子出现
</CastSection>
