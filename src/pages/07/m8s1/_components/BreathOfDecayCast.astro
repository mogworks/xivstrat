---
import Badge from '@/components/Badge.astro'
import CastSection from '@/components/section/CastSection.astro'
import type { Time } from '@/lib/utils'

import WolfOfWind from '../_components/entities/WolfOfWind.astro'

import { translations } from '../_translations'

interface Props {
  start: Time
  order: number
}

// 160000 8m宽直线 带伤害降低 可躲避

const ability = translations.breathOfDecay

const { start, order }: Props = Astro.props
---

<CastSection start={start} duration={7.7} ability={ability}>
  <WolfOfWind slot="source" order={order} />
  <Badge variant="emerald">8m宽直线</Badge>
</CastSection>
