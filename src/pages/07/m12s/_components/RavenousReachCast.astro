---
import Badge from '@/components/Badge.astro'
import Boss from '@/components/Boss.astro'
import CastSection from '@/components/section/CastSection.astro'
import type { Time } from '@/lib/utils'
import { translations } from '../_translations/cn'

interface Props {
  start: Time
}

const { start }: Props = Astro.props
const Lindwurm = translations.Lindwurm
const ravenousReach = translations.RavenousReach
---

<CastSection start={start} duration={11.7} ability={ravenousReach}>
  <Boss name={Lindwurm} slot="source" />
  <Badge variant="green">扇形</Badge>
  <Badge variant='purple'>超大范围AOE</Badge>
</CastSection>
