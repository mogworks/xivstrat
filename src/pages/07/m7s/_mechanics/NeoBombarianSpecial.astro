---
import type { Time } from '@/lib/utils'

import { Image } from 'astro:assets'

import MechanicSection from '@/components/section/MechanicSection.astro'
import SolutionSection from '@/components/section/SolutionSection.astro'

import BruteAbombinator from '../_components/BruteAbombinator.astro'
import NeoBombarianSpecialCast from '../_components/NeoBombarianSpecialCast.astro'

import img from '@/assets/07/m7s/NeoBombarianSpecial.png'

interface Props {
  base: Time
}

const { base = 0 }: Props = Astro.props
---

<NeoBombarianSpecialCast start="02:20.14" base={base} />
<MechanicSection>
  <div class="flex flex-col gap-4">
    <div class="gap-strat flex items-center">
      <BruteAbombinator />以自身为中心击飞所有玩家
    </div>
  </div>
</MechanicSection>
<SolutionSection>
  <div class="flex flex-col gap-4">
    <div class="gap-strat flex items-center">所有人在场地北侧集合被击飞到P2场地</div>
    <div class="gap-strat flex items-center">图中十字的交点为击飞的极限距离点，比此点更靠近boss会击飞后掉下平台</div>
    <div class="gap-strat flex items-center">站在点上会刚好落在平台边</div>
    <Image src={img} alt="" class="w-96 border-2 border-fuchsia-400 dark:border-fuchsia-100" />
  </div>
</SolutionSection>
