---
import type { Time } from '@/lib/utils'
import CastSection from '@/components/section/CastSection.astro'

import BruteAbombinator from './BruteAbombinator.astro'
import { translations } from '../_translations'

interface Props {
  start: Time
  duration: number
}

const { start, duration = 7.8 }: Props = Astro.props
const brutishSwing = translations.brutishSwing
---

<CastSection start={start} duration={duration} ability={brutishSwing}>
  <BruteAbombinator slot="source" />
  连线场地墙壁，随后移动至连线墙壁
</CastSection>
