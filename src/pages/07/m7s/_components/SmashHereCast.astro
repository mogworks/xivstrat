---
import type { Time } from '@/lib/utils'

import Badge from '@/components/Badge.astro'
import RoleIcon from '@/components/RoleIcon.astro'
import CastSection from '@/components/section/CastSection.astro'

import BruteAbombinator from './BruteAbombinator.astro'
import { translations } from '../_translations'

interface Props {
  start: Time
  base?: Time
}

const { start, base = 0 }: Props = Astro.props
const smashHereThere = translations.smashHereThere
---

<CastSection
  start={start}
  base={base}
  duration={4.7}
  ability={smashHereThere}
  target
  damage="730000"
  damageType="physical"
>
  <BruteAbombinator slot="source" />
  <Fragment slot="target">
    <RoleIcon role="all" tag="最近" />
    或
    <RoleIcon role="all" tag="最远" />
  </Fragment>
  <Badge variant="red">死刑</Badge>
  <Badge variant="emerald">分摊</Badge>
</CastSection>
