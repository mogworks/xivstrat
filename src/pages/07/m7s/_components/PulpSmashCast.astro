---
import type { Time } from '@/lib/utils'

import Badge from '@/components/Badge.astro'
import RoleIcon from '@/components/RoleIcon.astro'
import CastSection from '@/components/section/CastSection.astro'

import BruteAbombinator from './BruteAbombinator.astro'

interface Props {
  start: Time
  base?: Time
}

const { start, base = 0 }: Props = Astro.props
---

<CastSection start={start} base={base} duration={2.7} target ability="Pulp Smash" damage="100000" damageType="physical">
  <BruteAbombinator slot="source" />
  <RoleIcon role="healer" slot="target" />
  <Badge variant="pink">分摊</Badge>
</CastSection>
