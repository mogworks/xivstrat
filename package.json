{
  "name": "xivstrat",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "TURBO_TELEMETRY_DISABLED=1 turbo run dev",
    "prebuild": "TURBO_TELEMETRY_DISABLED=1 turbo run prebuild",
    "build": "TURBO_TELEMETRY_DISABLED=1 turbo run build",
    "preview": "TURBO_TELEMETRY_DISABLED=1 turbo run preview",
    "lint": "biome check",
    "format": "biome format --write",
    "clean": "pnpm dlx rimraf --glob \"apps/*/node_modules\" \"packages/*/node_modules\" node_modules pnpm-lock.yaml",
    "reinstall": "pnpm clean && pnpm install"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.9",
    "lefthook": "2.0.12",
    "turbo": "2.6.3",
    "typescript": "5.9.3"
  },
  "packageManager": "pnpm@10.26.0",
  "engines": {
    "node": ">=24"
  }
}
