{
  "name": "xivstrat",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "cross-env TURBO_TELEMETRY_DISABLED=1 turbo run dev",
    "prebuild": "cross-env TURBO_TELEMETRY_DISABLED=1 turbo run prebuild",
    "build": "cross-env TURBO_TELEMETRY_DISABLED=1 turbo run build",
    "preview": "cross-env TURBO_TELEMETRY_DISABLED=1 turbo run preview",
    "lint": "biome check",
    "format": "biome format --write",
    "clean": "pnpm dlx rimraf --glob \"apps/*/node_modules\" \"packages/*/node_modules\" node_modules pnpm-lock.yaml",
    "reinstall": "pnpm clean && pnpm install"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.9",
    "cross-env": "10.1.0",
    "lefthook": "2.0.12",
    "turbo": "2.6.3",
    "typescript": "5.9.3"
  },
  "packageManager": "pnpm@10.26.0",
  "engines": {
    "node": ">=24"
  }
}
