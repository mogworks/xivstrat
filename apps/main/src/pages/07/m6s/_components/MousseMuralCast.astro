---
import Badge from '@/components/Badge.astro'
import CastSection from '@/components/section/CastSection.astro'
import type { Time } from '@/lib/utils'
import { translations } from '../_translations'
import SugarRiot from './SugarRiot.astro'

interface Props {
  start: Time

  [key: string]: unknown
}

const { start, ...rest }: Props = Astro.props
const MousseMural = translations.MousseMural
---

<CastSection start={start} duration={4.7} ability={MousseMural} target damage="180000" damageType="magical" {...rest}>
  <SugarRiot slot="source" />
  <Badge variant="purple">AoE</Badge>
</CastSection>
