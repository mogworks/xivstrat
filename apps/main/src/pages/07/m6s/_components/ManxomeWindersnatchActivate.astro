---
import Bind from '@/components/buff/Bind.astro'

import DamageInfo from '@/components/DamageInfo.astro'
import RoleIcon from '@/components/RoleIcon.astro'
import Span from '@/components/Span.vue'
import EventSection from '@/components/section/EventSection.astro'
import type { Time } from '@/lib/utils'
import { translations } from '../_translations'
import Jabberwock from './Jabberwock.astro'

interface Props {
  time: Time

  wave: number
}

const { time, wave }: Props = Astro.props
const ManxomeWindersnatch = translations.ManxomeWindersnatch
---

<EventSection time={time}>
  <Jabberwock wave={wave} />
  对随机
  <RoleIcon role="healer" />
  释放
  <Span variant="yellow">{ManxomeWindersnatch}</Span>
  <DamageInfo damage="120000" type="magical" />
  <Bind hover />
</EventSection>
