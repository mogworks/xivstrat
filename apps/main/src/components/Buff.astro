---
import type { VariantType } from '@/lib/variant'

import VueBuff from '@/components/Buff.vue'
import VueHoverBuff from '@/components/HoverBuff.vue'
import CosImage from '@/components/CosImage.astro'

interface Props {
  icon: string
  name: string
  nameVariant?: VariantType
  description: string
  tag?: string
  hover?: boolean
  class?: string
}

const {
  icon,
  name,
  nameVariant = 'default',
  description,
  tag = '',
  hover = false,
  class: className = '',
}: Props = Astro.props
const iconUrl = new URL(icon, import.meta.env.PUBLIC_COS_BASE_URL).href
---

{
  hover ? (
    <VueHoverBuff
      icon={iconUrl}
      name={name}
      nameVariant={nameVariant}
      description={description}
      tag={tag}
      class={className}
      client:idle
    >
      <CosImage slot="icon" src={icon} alt={description} />
      <slot>
        <p>{description}</p>
      </slot>
    </VueHoverBuff>
  ) : (
    <VueBuff icon={iconUrl} name={name} description={description} tag={tag} class={className} client:idle>
      <CosImage src={icon} alt={description} />
    </VueBuff>
  )
}
